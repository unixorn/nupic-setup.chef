# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
  source ~/.bashrc
fi

export PATH=$PATH:/usr/local/bin:/usr/local/sbin
export PS1='[\u@\h:\w]\$ '

alias l='ls -la'

function hgrep {
  history | grep -i "$@"
}